language: python

cache:
  directories:
    - $GDALINST
    - ~/.cache/pip

env:
  global:
    - PIP_WHEEL_DIR=$HOME/.cache/pip/wheels
    - PIP_FIND_LINKS=file://$HOME/.cache/pip/wheels
    - GDALINST=$HOME/gdalinstall
    - GDALBUILD=$HOME/gdalbuild

  matrix:
    - GDALVERSION="1.11.5"
    - GDALVERSION="2.1.4"
    - GDALVERSION="2.4.0rc1"
    - GDALVERSION="trunk"
    
python:
  - "2.7"
  - "3.6"

matrix:
  fast_finish: true

  exclude:
  - env: GDALVERSION="trunk"
    python: "2.7"

  allow_failures:
    - env: GDALVERSION="trunk"

addons:
  apt:

    packages:
    - libproj-dev
    - libhdf5-serial-dev
    - libatlas-dev
    - libatlas-base-dev
    - gfortran

before_install:
  - pip install -U pip
  - pip install wheel coveralls>=1.1 --upgrade
  - pip install setuptools==36.0.1
  - . ./scripts/travis_gdal_install.sh
  - export PATH=$GDALINST/gdal-$GDALVERSION/bin:$PATH
  - export LD_LIBRARY_PATH=$GDALINST/gdal-$GDALVERSION/lib:$LD_LIBRARY_PATH
  - export GDAL_DATA=$GDALINST/gdal-$GDALVERSION/share/gdal
  - export PROJ_LIB=/usr/share/proj
  - gdal-config --version

install:
  - pip install --upgrade --force-reinstall -r requirements-dev.txt
  - pip uninstall -y fiona
  - if [ "$GDALVERSION" = "trunk" ]; then echo "Using gdal trunk"; elif [ $(gdal-config --version) == $(sed 's/[a-zA-Z].*//g' <<< $GDALVERSION) ]; then echo "Using gdal $GDALVERSION"; else echo "NOT using gdal $GDALVERSION as expected; aborting"; exit 1; fi
  - pip install --global-option=build_ext --global-option='-I$GDALINST/gdal-$GDALVERSION/include' --global-option='-L$GDALINST/gdal-$GDALVERSION/lib' --global-option='-R$GDALINST/gdal-$GDALVERSION/lib' -e .[test]
  - fio --version
  - gdal-config --version
  - fio --gdal-version

script:
  - python -m pytest -m "not wheel" --cov fiona --cov-report term-missing

after_success:
  - coveralls || echo "!! intermittent coveralls failure"

before_cache:
  - if [ "$GDALVERSION" = "trunk" ]; then rm -rf $GDALINST/gdal-$GDALVERSION; fi


git:
  depth: 3
  quiet: true
  
jobs:
  include:

    - stage: Gdal legacy
      python: 2.7
      env: GDALVERSION="1.9.2"

    - stage: Gdal legacy
      python: 3.6
      env: GDALVERSION="1.9.2"
      
    - stage: Gdal legacy
      python: 2.7
      env: GDALVERSION="1.10.1"

    - stage: Gdal legacy
      python: 3.6
      env: GDALVERSION="1.10.1"

    - stage: Gdal legacy
      python: 2.7
      env: GDALVERSION="2.0.3"
    
    - stage: Gdal legacy
      python: 3.6
      env: GDALVERSION="2.0.3"
    
    - stage: Gdal legacy
      python: 2.7
      env: GDALVERSION="2.2.4"

    - stage: Gdal legacy
      python: 3.6
      env: GDALVERSION="2.2.4"

    - stage: Gdal legacy
      python: 2.7
      env: GDALVERSION="2.3.3rc2"

    - stage: Gdal legacy
      python: 3.6
      env: GDALVERSION="2.3.3rc2"

      
    #- stage: Gdal trunk
      #python: 2.7
      #env: GDALVERSION="trunk"

    #- stage: Gdal trunk
      #python: 3.6
      #env: GDALVERSION="trunk"
      
stages:
  - test

  - name: Gdal legacy
    if: commit_message =~ /@ci_legacy/ OR commit_message =~ /@ci_all/

  #- name: Gdal trunk
    #require the branch name to be master (note for PRs this is the base branch name)
    #if: branch = master OR commit_message =~ /@ci_trunk/ OR commit_message =~ /@ci_all/
